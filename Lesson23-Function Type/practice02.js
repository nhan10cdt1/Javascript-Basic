/**
  Expreesstion funtion 
  - ※(not hosting)Có thể đặt tên cho hàm hoặc là không
  - Nhưng thường đặt tên cho dễ hiểu trong quá trình code
 */

var showMessage = function() {
    console.log("Expression function");
};

//callback function
setTimeout(function() {}, 3000);

var myObject = {
    myFuntion: function() {},
};