/**
   Declaration function 
   - Bắt buộc phải đặt tên
   - Hổ trợ hosting 
   Có thể gọi hàm trước khi định nghĩa 
 */
showMessage();

function showMessage() {
    console.log("Declaration function");
}