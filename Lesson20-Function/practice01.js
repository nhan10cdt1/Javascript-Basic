// Sử dụng từ khóa funtion
// Quy tắc đặt tên hàm

function showDialog() {
    alert("Hi xin chào tất cả các ban !");
}

// Toán tử  call() để gọi hàm
showDialog();

// Truyền tham số cho hàm
function writelog(message, type) {
    console.log(message);
}